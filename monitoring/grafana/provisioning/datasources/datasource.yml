apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # 대시보드 패널이 이 고정 UID로 참조한다.
    access: proxy
    # 같은 compose 네트워크의 Prometheus 서비스. 컨테이너 내부 포트 9090.
    url: http://prometheus:9090
    isDefault: true
    editable: true
